# Лабораторная работа 3. Условия и логические выражения

[План курса](https://github.com/vvdRTM/Python2026) · Предыдущая: [Вычисления](02_Вычисления.md) · Следующая: [Циклы](04_Циклы.md)

## Цель

Строить программы с несколькими путями выполнения и проверять границы условий. Необходимы работы №1–2.

## Краткая теория

Сравнения `==`, `!=`, `<`, `<=`, `>`, `>=` возвращают `True` или `False`. `=` присваивает значение, `==` сравнивает. Числа нужно сравнивать как числа: строки `"10"` и `"2"` сравниваются лексикографически, поэтому `"10" < "2"` истинно.

- `and`: оба условия истинны.
- `or`: хотя бы одно условие истинно.
- `not`: отрицание условия.
- `1 <= value <= 3`: значение принадлежит отрезку, включая обе границы.

```python
# example.py
score = int(input("Баллы от 0 до 100: "))
if score < 0 or score > 100:
    print("Ошибка: баллы вне диапазона")
elif score >= 80:
    print("Высокий результат")
elif score >= 50:
    print("Зачёт")
else:
    print("Нужна доработка")
```

Выполняется только первая подходящая ветвь цепочки. При `score=80` выводится `Высокий результат`. Если вместо `elif` написать отдельные `if`, несколько блоков могут выполниться за один запуск.

После условия ставится двоеточие, тело блока оформляется одинаковыми отступами. В курсе используйте четыре пробела на уровень, без смешивания пробелов и табуляции.

## Обязательные задания

### 1. Минимум трёх чисел

В `minimum.py` введите три целых числа и найдите минимальное через `if`/`elif`/`else` или последовательные проверки. Функции `min`, `max` и сортировку не используйте: цель — потренировать ветвление. Равные числа допускаются.

### 2. Калькулятор

В `calculator.py` введите два вещественных числа и строку с одной операцией: `+`, `-`, `*`, `/`. Выведите результат с двумя знаками после точки. При делении на ноль выведите `Деление на ноль запрещено`, при другой операции — `Неизвестная операция`. Не используйте `eval()`.

### 3. Принадлежность прямоугольнику

В `point.py` введите координаты `x`, `y`. Прямоугольник задан условиями `0 <= x <= 5` и `0 <= y <= 3`. Выведите `Внутри или на границе` либо `Снаружи`. Граница включена.

## Индивидуальный вариант

В `variant.py` введите целое число от 0 до 100. При выходе за диапазон выведите `Ошибка диапазона`. Иначе определите категорию по таблице. Все интервалы включают указанные концы.

| Вариант | Смысл числа | Низкая категория | Средняя категория | Высокая категория |
| --- | --- | --- | --- | --- |
| 1 | Заряд аккумулятора | 0–19: `Низкий` | 20–79: `Средний` | 80–100: `Высокий` |
| 2 | Заполненность хранилища | 0–49: `Свободно` | 50–89: `Мало места` | 90–100: `Почти заполнено` |
| 3 | Выполнение учебного плана | 0–39: `Начало` | 40–99: `В процессе` | 100: `Завершено` |
| 4 | Результат теста | 0–49: `Нужна доработка` | 50–84: `Зачёт` | 85–100: `Отличный результат` |
| 5 | Заполнение резервуара | 0–24: `Мало` | 25–74: `Достаточно` | 75–100: `Много` |
| 6 | Загрузка процессора | 0–29: `Низкая` | 30–69: `Средняя` | 70–100: `Высокая` |
| 7 | Процент прочитанной книги | 0–9: `Начало` | 10–89: `Чтение` | 90–100: `Почти готово` |
| 8 | Заполнение группы | 0–39: `Есть места` | 40–79: `Группа набирается` | 80–100: `Почти заполнена` |
| 9 | Уровень игрового ресурса | 0–14: `Пополнить` | 15–59: `Достаточно` | 60–100: `Большой запас` |
| 10 | Прогресс загрузки файла | 0–4: `Начало` | 5–94: `Загрузка` | 95–100: `Завершение` |
| 11 | Заполненность парковки | 0–59: `Много мест` | 60–89: `Мало мест` | 90–100: `Почти занята` |
| 12 | Условный уровень сигнала | 0–34: `Слабый` | 35–74: `Средний` | 75–100: `Сильный` |
| 13 | Доля выполненных задач | 0–24: `Начало` | 25–84: `В процессе` | 85–100: `Завершение` |
| 14 | Наличие товара от плана | 0–19: `Малый запас` | 20–64: `Средний запас` | 65–100: `Большой запас` |
| 15 | Заполнение архива | 0–44: `Есть место` | 45–94: `Архив растёт` | 95–100: `Почти заполнен` |

## Проверки

- Минимум: `(2, 10, 3)` → `2`; `(5, 5, 5)` → `5`; `(-4, 0, -2)` → `-4`. Добавьте случай с минимумом на последней позиции.
- Калькулятор: `7 / 2` → `3.50`; `5 / 0` → сообщение; `5 ? 2` → сообщение. Проверьте остальные три операции.
- Точка: `(2, 2)` → внутри; `(5, 3)` → на границе, тоже подходит; `(5.1, 3)` → снаружи.
- Вариант: проверьте `-1`, `0`, `100`, `101` и значения непосредственно до и после каждого порога.

## Сдача и защита

Сдайте четыре исходника и README с таблицей «ввод → ожидаемый ответ → фактический ответ». Критерии — в работе №1. На защите объясните выбор `and`/`or`, порядок ветвей и поведение при равенстве значений.

Дополнительно: определите, является ли год високосным по правилу григорианского календаря: делится на 400 либо делится на 4, но не на 100. Вход — целый год от 1 до 9999. Проверьте 1900, 2000, 2024 и 2023; ответы: нет, да, да, нет.

Справка: [оператор if](https://docs.python.org/3/tutorial/controlflow.html#if-statements).
